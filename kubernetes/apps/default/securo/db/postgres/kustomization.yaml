---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
# Init-only pgvector/grant SQL dropped with the move to recovery-by-default;
# on a from-scratch rebuild add it under the initdb override (see component README).
